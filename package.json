{
  "name": "@karinjs/puppeteer-repo",
  "version": "2.1.2",
  "scripts": {
    "build": "pnpm -r build",
    "build:dev": "pnpm build:minify",
    "build:minify": "pnpm -r build -- --false",
    "dev": "pnpm -r dev",
    "pub": "pnpm -r pub",
    "rm": "pnpm -r run rm",
    "sort": "sort-package-json && pnpm -r exec sort-package-json",
    "sync": "pnpm -r sync",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r test:coverage",
    "test:watch": "pnpm -r test:watch",
    "up-version": "node up-version.cjs",
    "web": "tsx packages/puppeteer-server/src/index.ts"
  },
  "devDependencies": {
    "@types/decompress": "^4.2.7",
    "@types/node": "^22.5.4",
    "cross-env": "^7.0.3",
    "devtools-protocol": "^0.0.1433962",
    "eslint": "latest",
    "neostandard": "latest",
    "puppeteer-core": "^24.4.0",
    "sort-package-json": "^3.0.0",
    "terser": "^5.39.0",
    "tsup": "^8.4.0",
    "typed-query-selector": "^2.12.0",
    "typescript": "5.4.5",
    "vite": "^6.3.2"
  }
}